<h2> My Boards</h1>

<ul>
	<% boards.each(function (board) { %>
			<li class="board-index" data-id="<%= board.id %>">
				  <%= board.escape("title") %>
			</li>
	<% }) %>
	<li id="new-board" class="board-index board-new"> Create new board... </li>
</ul>
